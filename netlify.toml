[build]
  publish = "."
  command = ""

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."syncDealsFinal"]
  schedule = "@hourly"

# 🕰️ Runs every 5 minutes from 11:00 to 21:55 UTC (6am–9pm Chicago time)
[functions."syncLeaderboardStats"]
  schedule = "*/5 11-21 * * *"
