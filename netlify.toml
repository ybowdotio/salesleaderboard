[build]
  publish = "."
  command = ""  # leave blank or "npm run build" if needed later

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."syncDeals"]
  schedule = "@hourly"

